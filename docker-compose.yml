version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventory-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - ENV=production
    restart: unless-stopped
